#include <stdio.h>
int main()
{
    int a = 33;
    int b = 4;
    int c = a % b;
    // float c = 45.36/5;
    printf("Addition of %d and %d is %d", a, b, c);
    return 0;
}
/*
Opeartors
2+3
operands----value or variable at which operation perform

operator + - * / > < >= <= >>>>>>>

expression

Types of operators

increment or decrement operator ++ --
arithmetic operators + - * / %(Modulus)
relational operators > < >= < = == !=
Logical Operators AND(&&) OR(||) NOT(!)
assignement operators = += -= *= /=
bitwise operator & | >> <<
miscelleanous operator ?:(Ternary operator) *pointer sizeof()

typecasting ---changing one datatype into another for a moment

explicit type casting

impliciting type casting
(datataype)variable

*/
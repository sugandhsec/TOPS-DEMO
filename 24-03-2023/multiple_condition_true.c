#include <stdio.h>
int main()
{
    int num;
    printf("Enter Number: ");
    scanf("%d", &num);
    if (num < 100)
    {
        printf("less than 100");
    }
    else if (num < 100)
    {
        printf("Smaller than 100");
    }
    else if (num < 100)
    {
        printf("!00 smaller");
    }

    return 0;
}